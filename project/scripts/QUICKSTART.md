# 🚀 BTC 预测服务 - 快速开始

## ✅ 已完成功能

1. ✅ **实时数据监控** - 从 Binance 获取多时间框架 K 线数据
2. ✅ **GBM 模型预测** - 使用训练好的 GBM 模型进行预测
3. ✅ **每小时自动预测** - 每小时整点自动执行预测
4. ✅ **Telegram 通知** - 通过 Telegram 机器人发送预测报告
5. ✅ **预测报告包含**:
   - 📈 涨跌方向 (看涨/看跌/震荡)
   - 📊 涨跌区间 (大跌/小跌/横盘/小涨/大涨)
   - 🎯 置信度 (10-90%)
   - 💰 当前价格和资金费率

## 🎯 快速测试

```bash
cd project/scripts

# 测试模式 (执行一次预测后退出)
python3 prediction_server.py --model ../models/regression_model_20251213_213205.pkl --test
```

## 📱 配置 Telegram

1. **创建机器人**:
   - 在 Telegram 搜索 `@BotFather`
   - 发送 `/newbot` 创建机器人
   - 获取 Bot Token

2. **获取 Chat ID**:
   ```bash
   # 发送消息给你的机器人后，访问:
   curl https://api.telegram.org/bot<YOUR_TOKEN>/getUpdates
   # 在返回的 JSON 中找到 "chat":{"id": 123456789}
   ```

3. **运行服务**:
   ```bash
   # 方法1: 环境变量
   export TELEGRAM_BOT_TOKEN="your_token"
   export TELEGRAM_CHAT_ID="your_chat_id"
   python3 prediction_server.py --model ../models/regression_model_20251213_213205.pkl

   # 方法2: 命令行参数
   python3 prediction_server.py \
     --model ../models/regression_model_20251213_213205.pkl \
     --telegram-token "your_token" \
     --telegram-chat-id "your_chat_id"
   ```

## 🔄 后台运行

```bash
# 使用部署脚本
cd deploy
chmod +x start.sh stop.sh

# 启动 (后台)
./start.sh --background

# 停止
./stop.sh
```

## 📊 预测报告示例

```
🔮 BTC 价格预测报告
━━━━━━━━━━━━━━━━━━━━━━━━

📅 预测时间: 2024-12-14 13:55
🎯 预测目标: 2024-12-15 09:55 (20h后)

💰 当前价格: $90,151.00
💵 资金费率: 0.0060%

━━━━━━━━━━━━━━━━━━━━━━━━
📊 预测结果

📉 方向: 看跌
🔴 区间: 小跌 (-2% ~ -0.5%)
📈 预测涨跌: -1.70%
🎯 置信度: 53%

━━━━━━━━━━━━━━━━━━━━━━━━
📈 市场状态

📊 信号强度: -2
📉 RSI(1h): 45.3
📈 ADX(1h): 22.5
⚡ 波动率: 1.15%

━━━━━━━━━━━━━━━━━━━━━━━━
⚠️ 仅供参考，不构成投资建议
```

## ⚙️ 服务器部署

详细部署指南请查看: `deploy/README.md`

### Systemd 服务 (推荐)

```bash
sudo cp deploy/btc-predictor.service /etc/systemd/system/
sudo nano /etc/systemd/system/btc-predictor.service  # 编辑配置
sudo systemctl daemon-reload
sudo systemctl enable btc-predictor
sudo systemctl start btc-predictor
sudo systemctl status btc-predictor
```

## 📝 注意事项

1. **模型路径**: 确保模型文件路径正确
2. **网络连接**: 需要能够访问 Binance API
3. **Telegram 配置**: 可选，不配置时仅输出到控制台
4. **预测频率**: 当前固定为每小时整点，如需修改请编辑代码

## ❓ 常见问题

**Q: 如何更换模型？**
```bash
python3 prediction_server.py --model ../models/your_model.pkl --test
```

**Q: 如何修改预测频率？**
编辑 `prediction_server.py` 中的 `schedule.every().hour.at(":00")` 部分

**Q: 预测不准确？**
- 使用更多历史数据重新训练模型
- 尝试不同的模型 (RF, LSTM, Ensemble)
- 调整置信度阈值

